# 版本歷史

> 版號規則見 CLAUDE.md：`breaking` → major 升版、build 歸零；`feat`/`fix`/`refactor` → build 遞增。
> 此檔案的版號必須與 `nl-to-dax/VERSION` 一致。
> 本檔案記錄 `mcp-oauth` 分支的歷史；`solo`／`server-token` 各自維護自己的版本線。

## v1.1（目前版本）— 2026-08-10

### 自動更新
- 新增 `config/update_source.json`（範本 `update_source.json.example`）：來源倉庫網址改為部署設定，不再寫死在 `check_update.py` 裡——同一份 skill 可能發佈自 GitHub、自架 Gitea 或內部鏡像
- 新增 `update_skill.py`：淺層 clone 指定 tag 後覆蓋本機程式碼。部署方式是複製目錄、通常不是 git working tree，因此不能用 `git pull`
- **只覆蓋程式碼**（`SKILL.md`／`VERSION`／`scripts/`／`config/*.example`）；`config/` 底下任何非 `.example` 的 `.json` 一律視為使用者資料，絕不覆蓋或刪除
- 覆蓋前先備份，任一步失敗即整批還原，不會停在「更新到一半」的壞掉狀態
- **必須經使用者同意才執行**：偵測到新版只提示並詢問，不自動更新——那等於在 skill 執行到一半改寫自己的檔案。更新後新版指令要下次對話才生效
- 未設定 `repo_url` 時，版本檢查與更新皆靜默略過，不影響主流程

---

## v1.0 — 2026-08-10

首個以 MCP connector + OAuth 為認證基礎的版本，與先前 `PBI_MASK_KEY` 架構完全不相容。

### 認證架構（breaking）
- 移除 `PBI_MASK_KEY` 與所有本機憑證管理：不再有 `settings.local.json`、`fetch_credential.py`、`fetch_model.py`、`check_setup.py`、`skill_settings.py`、`pbi_api_client.py`、`model_overview.py`
- 改由 MCP connector 提供 `list_models`／`get_model_detail`／`get_query_ticket` 三支工具，Skill 不接觸任何 Azure AD 憑證
- 語意模型結構與篩選規則改為即時向 Server 取得，不再有本機 `pbi_config/` 快取與每日強制同步機制
- 篩選設定檔從本機 `filters/*.json` 改為 `get_model_detail` 回傳的 `filters` 欄位，由管理員於申請程式集中維護；比對演算法不變

### 查詢授權：一次性 ticket（breaking）
- `get_powerbi_token` 已被 Server 移除，改為 `get_query_ticket`：回傳單次使用、預設 300 秒效期的 ticket 與 `redeem_url`
- `execute_dax_query.py` 自行以 ticket 兌換 access token，**token 全程只存在該行程記憶體**——不進對話上下文、不進命令列、不落地成檔案
- Token 快取指引反轉：ticket 單次使用，每次查詢前都要重新取得，不快取
- 兌換回 401 時輸出 `"ticket_expired": true`，由 Skill 重新取票後重跑一次（Bash 權限確認的等待時間會算進 ticket 效期，過期屬正常情況）
- Network egress 需放行兩個網域：`api.powerbi.com` 與申請程式網域

### 查詢書籤
- 新增 `bookmarks.py`：查詢成功後可命名存檔，下次執行時列於模型概覽最下方
- 同時保存 DAX 與原始自然語言需求，重用時可選「直接執行」或「用原需求重新生成」，後者可避開書籤 DAX 寫死日期、或管理員調整過規則導致的過時結果
- 依模型分組，避免列出跑不動的跨模型查詢
- 環境持久性偵測：探針檔跨天存活為正面證明，首次執行退回路徑判斷；沙盒環境仍可儲存但會明確告知僅單次對話有效

### 啟動檢查與跨平台
- 新增 `preflight.py`：Python 環境、版本更新、查詢書籤三項檢查併為單一指令，SKILL.md 相應精簡
- 版本閘門置於其餘 `import` 之前，確保舊版 Python 得到的是「請升級」而非 traceback；各區段獨立容錯
- **移除全部 `trigger_*.ps1`／`.sh` 觸發腳本**：Windows 用戶端版 PowerShell ExecutionPolicy 預設 `Restricted` 會直接擋掉未簽署的 `.ps1`，wrapper 反而製造了直接呼叫 Python 不會有的失敗點；改由 Claude 直接以 `python`／`python3` 呼叫

### 其他
- 支援 `query_modes`（多查詢模式時先詢問使用者）與 `column_aliases`（生成 DAX 前把口語用詞轉為實際欄位值），皆為純增量——Server 尚未回傳這些欄位時自動略過
- 補上不支援 OAuth connector 介面的 AI App 引導流程（改用申請程式簽發的 MCP token 手動設定）

---

## v3 — 2026-06-22（`PBI_MASK_KEY` 架構，已淘汰）

> 此版本屬於舊的版號體系（v1/v2/v3），與現行 `Major.Build` 規則不同；`mcp-oauth` 分支自 v1.0 起重新起算。

- `filters/` 篩選設定檔系統：以 JSON 定義業務規則，支援 `alwaysApply` 與 `overrideDefaults`
- `chunk_model.py` 雙格式自動偵測：原始 Power BI JSON 與簡化語意模型格式
- `pbi_api_client.py` JWT 憑證驗證（HS256 + `PBI_MASK_KEY`）與過期檢查
- Windows／macOS／Linux 三平台觸發腳本
