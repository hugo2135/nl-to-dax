# Power BI 設定檔說明
#
# 向供應方申請後，會取得一個 JWT 格式的純文字設定檔。
# 將該檔案命名為 pbi_credentials.jwt 並放在此資料夾中。
#
# JWT 格式：<header_b64>.<payload_b64>.<signature_b64>
#
# Payload 必須包含以下欄位：
#   tenant_id      - Azure AD 租用戶 ID
#   client_id      - 應用程式（服務主體）ID
#   client_secret  - 應用程式密碼
#   workspace_id   - Power BI 工作區 ID
#   dataset_id     - 語意模型（資料集）ID
#   iss            - 簽發者（固定值："pbi-skill-provider"）
#   iat            - 簽發時間（Unix timestamp）
#   exp            - 過期時間（Unix timestamp）
#
# 使用 HS256 演算法簽名，簽名金鑰為環境變數 PBI_MASK_KEY。
#
# 設定 PBI_MASK_KEY：
#   Windows PowerShell: $env:PBI_MASK_KEY = "your-secret-key"
#   macOS/Linux:        export PBI_MASK_KEY="your-secret-key"
